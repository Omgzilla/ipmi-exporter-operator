options:
  ipmi-exporter-version:
    type: string
    default: "v1.8.0"
    description: Version of IPMI Exporter to install.
  listen-address:
    type: string
    default: "0.0.0.0:9290"
    description: Address to listen to. 
